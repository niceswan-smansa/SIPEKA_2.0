# Feature modules

Future phases add feature modules here. Each module exposes cross-module dependencies through
`index.ts` and keeps `domain`, `application`, `infrastructure`, and `presentation` separate.
Phase 0 intentionally contains no product feature modules.
